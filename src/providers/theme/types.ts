export enum fillOptions {
    fill = "fill",
    link = "link",
    outline = "outline",
    shadow = "shadow"
}

export enum colorOptions {
    primary = "primary",
    danger = "danger",
    warning = "warning",
    success = "success"
}

export enum componentSizeOptions {
    small = "small",
    medium = "medium",
    large = "large"
}
